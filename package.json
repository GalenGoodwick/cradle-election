{
  "name": "cradle-election",
  "version": "0.1.0",
  "description": "The Cradle — an autonomous development director. Project memory competes in a recused evaluator tournament; the champion sets development direction and seeds every relaunch.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/run.ts",
    "boot": "tsx src/boot-cli.ts",
    "state": "tsx src/state.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
